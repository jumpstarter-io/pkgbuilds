#Maintainer: Matt Monaco <net 0x01b dgbaley27>

pkgname=js-dep
pkgver=1
pkgrel=4
arch=(any)
pkgdesc="This is a meta package that install all Jumpstarter dependencies in a production cluster"
url=http://jumpstarter.io
license=(NONE)
#source=(git://github.com/jumpstarter-io/ceph-deploy.git#branch=$repobranch)
#sha256sums=(SKIP)
depends=(
	python2
	python2-setuptools
	python
	ipvsadm
	dnsutils
	python2-aws-cli
	emacs-nox
	htop
	linux-js
	linux-js-kdump
	ceph-deploy
	ceph
	nginx
	nodejs
	postgresql
	tmux
	fish
	traceroute
	python2-pyudev
	python-pyudev
)

#makedepends=(git python2-setuptools)

#package() {
#	cd "$srcdir/$pkgname"
#	python2 setup.py install --root="$pkgdir/" --optimize=1
#	install -m644 -D LICENSE "$pkgdir/usr/share/licenses/$pkgname/LICENSE"
#}
